{
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Bash(git diff *)",
      "Bash(git log *)",
      "Bash(git status *)",
      "Bash(git branch *)",
      "Bash(git show *)"
    ]
  },
  "agentGroups": {
    "quality": {
      "description": "Code quality agents: review, test, refactor, security",
      "agents": [
        "code-reviewer",
        "test-generator",
        "refactorer",
        "security-auditor"
      ]
    },
    "ops": {
      "description": "Operations agents: dependency audit, migration planning, documentation",
      "agents": [
        "dependency-auditor",
        "migration-planner",
        "documentation-writer"
      ]
    }
  },
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "which osascript >/dev/null 2>&1 && osascript -e 'display notification \"Claude Code needs attention\" with title \"Claude Code\"' || true"
          }
        ]
      }
    ]
  }
}
